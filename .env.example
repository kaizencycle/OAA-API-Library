# File persistence (defaults to project root OAA_MEMORY.json if unset)
OAA_MEMORY_PATH=./OAA_MEMORY.json

# HMAC secret for POSTs (required for writes)
MEMORY_HMAC_SECRET=change-me

# HMAC to accept Eve's POST (writer auth)
EVE_HMAC_SECRET=change-me

# Ledger (mock or prod)
LEDGER_BASE_URL=http://localhost:4000
LEDGER_ADMIN_TOKEN=dev-token

# Dev mode
DEV_MODE=1